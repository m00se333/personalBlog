extends ./layout
block content

	include ./partials/_nav.jade

	div(id="postHolder")
		for post in posts
				div.post
					h2 #{post.title}
					p #{post.body + ".."}
					a(href="/blog/" + post.title)
						button Read More
					div(class="fadeBottom")
